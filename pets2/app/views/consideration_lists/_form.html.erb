<%= form_for(@consideration_list) do |f| %>
  <% if @consideration_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consideration_list.errors.count, "error") %> prohibited this consideration_list from being saved:</h2>

      <ul>
      <% @consideration_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
