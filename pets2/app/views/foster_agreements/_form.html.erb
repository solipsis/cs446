<%= form_for(@foster_agreement) do |f| %>
  <% if @foster_agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@foster_agreement.errors.count, "error") %> prohibited this foster_agreement from being saved:</h2>

      <ul>
      <% @foster_agreement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
